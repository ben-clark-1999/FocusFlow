import { describe, it, expect } from 'vitest';

// Smoke test placeholder so CI passes without WebAudio in Node
describe('audio init', () => {
  it('engine exists (placeholder)', () => {
    expect(true).toBe(true);
  });
});
